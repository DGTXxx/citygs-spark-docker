{
  "name": "citygs-spark-docker",
  "private": true,
  "version": "0.1.0",
  "description": "SparkJS browser-side 3D Gaussian Splatting viewer packaged for Docker handoff.",
  "workspaces": ["frontend"],
  "scripts": {
    "dev": "npm --workspace @citygs-spark/frontend run dev",
    "build": "npm --workspace @citygs-spark/frontend run build",
    "typecheck": "npm --workspace @citygs-spark/frontend run typecheck"
  },
  "engines": { "node": ">=20" }
}
