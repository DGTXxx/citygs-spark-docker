.git
node_modules
*/node_modules
frontend/dist
logs
*.log
*.tsbuildinfo

# Keep the frontend build context small. The model assets are packed by the
# separate model-image Docker build whose context is frontend/public.
frontend/public/models
